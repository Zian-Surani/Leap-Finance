# ui/metrics.py

import streamlit as st


def render_metrics_mapping():
    st.subheader("Product Metrics Mapping")

    st.markdown("""
**Sessions per user (first 4 weeks)**  
→ External effort validation pull + escrow recovery windows

**Course completion rate**  
→ Commitment bands + irreversible benefit expiry

**Motivation & momentum**  
→ Preparation Capital ledger + forward forecast

**Retention quality (not vanity retention)**  
→ Loss of escrowed leverage instead of streak resets
""")
